<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusGear"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Auth Feature: Login Screen (Launcher) -->
        <activity
            android:name=".feature.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Feature: Register Screen -->
        <activity
            android:name=".feature.auth.RegisterActivity"
            android:exported="false" />

        <!-- Home Feature: Home Screen (Post-login) -->
        <activity
            android:name=".feature.home.HomeActivity"
            android:exported="false" />

    </application>

</manifest>
